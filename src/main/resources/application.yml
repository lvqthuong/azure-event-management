spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wade
    username: wade
    password: wade
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update